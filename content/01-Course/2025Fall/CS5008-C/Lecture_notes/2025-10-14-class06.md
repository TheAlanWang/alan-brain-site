Breaking Down Expressions
`2 + 3 * 4 - 5`

### The Shunting Yard Algorithm
Dijkstra
### Fix it After

```c
#include <stdio.h>

typedef struct Node {
    int data;
    struct Node *lhs;
    struct Node *rhs;
} Node;
```

### `calloc`
```c
Node *node = calloc(1, sizeof(Node));
```

`void *calloc(size_t num_elements, size_t element_size);`
- Allocate **one element**, where each element has the size of a `Node` struct.

### EBNF
```EBNF
expr   = term, {addop, term};
term   = factor, {mulop, factor};
factor = number | "(", expr, ")";
addop  = "+" | "-";
mulop  = "*" | "/";
```

`{}` -> repetition brackets
### Pratt Parsing
- A Pratt Parser is a kind of Top-Down Operator Precedence Parser.
Building Tree Left-Associative and Right-Associative
when precedence higher, 

```c
void wrong_rotate(Node *root) {
    root = root->lhs;  // only changes local copy
}

void correct_rotate(Node **root) {
    *root = (*root)->lhs;  // changes the caller’s root
}
```

## Stack Machine
we can compile our code to a stack machine program rather than directly x86-64
