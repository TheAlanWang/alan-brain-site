Amazon ElastiCache 
- is AWS’s managed in-memory data store / cache service
- most commonly used to run **Redis (Redis OSS) / Valkey / Memcached** without you managing servers, patching, failover, etc.